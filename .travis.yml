dist: trusty
sudo: required
language: cpp
compiler: gcc
addons:
 apt:
  packages:
   - gcc
   - g++
   - cmake

script:
 - mkdir build
 - cd build
 - cmake -DAABB_BUILD_TESTS=ON ..
 - make
 - make test
